server:
  port: 5000
  session: 
    cookie:
      name: gfresh-az
spring:
  application:
    name: eureka-zuul
  session:
    store-type: redis
    redis: 
      namespace: auth-
      flush-mode: on-save
eureka:
  instance: 
    preferIpAddress: true
    nonSecurePort: 5000
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/       